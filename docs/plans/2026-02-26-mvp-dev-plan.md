# 🚀 [Project DESTINY] MVP 前端與全端整合開發計畫 (Dev Plan)

**文件目標：** 本文件是基於 `2026-02-26-mvp.md` 規格與 `TECH-STACK.md` 現有架構，提供給 AI 開發者（如 Cursor 等）的具體實作藍圖。目標是搭建塔羅牌視覺體驗的前端 MVP，並串接現有的 Supabase 與 Python API。

---

## 🏗️ 1. 系統架構與技術棧盤點

- **Frontend:** Next.js 16 (App Router), React 19
- **Styling:** Tailwind CSS v4, shadcn/ui
- **Animations:** framer-motion (3D 卡牌翻轉效果)
- **Auth & DB:** Supabase SSR (`@supabase/ssr`)
- **Backend API:** Python FastAPI (運行於 `http://localhost:8001`)

---

## 🛠️ 2. Supabase 與 Google OAuth 登入設定

MVP 的核心是無摩擦的「Google 一鍵登入」。

### 2.1 Supabase Dashboard 設定
- [ ] 進入 Supabase Dashboard -> Authentication -> Providers。
- [ ] 啟用 **Google Provider**。
- [ ] 輸入從 Google Cloud Console 取得的 `Client ID` 與 `Client Secret`。
- [ ] 確保 Google Cloud Console 中的「已授權的重新導向 URI」包含 `https://<your-supabase-project>.supabase.co/auth/v1/callback`。

### 2.2 Next.js 登入實作 (Auth Flow)
- **套件:** 確認已安裝 `@supabase/ssr` 和 `@supabase/supabase-js`。
- **UI 元件 (`src/app/login/page.tsx`):** 實作一個帶有神秘星空背景的登入頁面，核心按鈕為 `<Button onClick={handleGoogleLogin}>Continue with Google</Button>`。
- **登入邏輯:**
  ```typescript
  const handleGoogleLogin = async () => {
    const supabase = createBrowserClient(
      process.env.NEXT_PUBLIC_SUPABASE_URL!,
      process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
    )
    await supabase.auth.signInWithOAuth({
      provider: 'google',
      options: {
        redirectTo: `${location.origin}/auth/callback`
      }
    })
  }
  ```
- **Callback 路由 (`src/app/auth/callback/route.ts`):** 實作 Supabase 官方標準的 PKCE callback 路由，登入成功後重導向至首頁 (`/lounge`)。

---

## 🗄️ 3. 資料庫 Schema 擴展 (Token & 暫存快取)

請透過 Supabase SQL Editor 或 Migration 腳本建置 MVP 所需的表格結構（參考 `2026-02-26-mvp.md`）：

1. **`users` 擴充:** 新增 `tokens INT DEFAULT 3` 欄位。
2. **`soul_cards`:** 儲存用戶自建的命盤卡片 (`birth_data` 與預算好的 `natal_cache`)。
3. **`matches` (合盤報告):** 儲存雙人合算的結果，包含 `user_a_id`, `user_b_id`, `harmony_score` 與 LLM 產出的 JSON。

> **實務建議：** 寫一個 Supabase RPC (Stored Procedure) `record_match_and_deduct_token`，讓扣除代幣與新增 Match 紀錄能在同一個 Transaction 內完成，確保代幣不會超扣。

---

## 🔮 4. 核心 UI: 塔羅牌沉浸式體驗 (Framer Motion)

### 4.1 3D 卡牌元件 (`<TarotCard />`)
- **目標:** 實作可點擊翻面的虛擬塔羅牌。
- **技術:** 使用 Tailwind 的 `perspective`, `backface-visibility: hidden` 搭配 `framer-motion` 的 `animate={{ rotateY: isFlipped ? 180 : 0 }}`。

**正面 (Light Side):**
- 顯示關係原型 Tag (Archetype)、正面引力 (Resonance)。
- 視覺風格: 明亮、星空紋理、象徵連結的 Icon。

**背面 (Dark Side - 旋轉 180 度):**
- 顯示權力深淵 (Shadow) 與絕對死穴 (Toxic Traps)。
- 視覺風格: 偏暗黑、霓虹紫暈影、警告意味的 UI 設計。

### 4.2 配對大廳與動態輸入防呆 (`src/app/lounge/page.tsx`)
1. 選擇兩張卡片（或手動輸入雙方生日）。
2. **出生地 Autocomplete (Google Places/Mapbox):** 確保傳給後端的 `lat`, `lng` 與時區絕對精準，避免手動輸入導致星曆表解析錯誤。
3. **「未知時間」容錯防呆:** 提供 Checkbox 讓不知道幾點出生的人勾選 (Tier 3)，後端自動排除上升與月亮度數的計算。
4. 發送配對請求給 `astro-service`。
5. **Loading 動畫:** 必須實作「星盤轉動」或「塔羅洗牌」的過場動畫，營造命理儀式感。
6. 渲染 3D 塔羅牌群組。

---

## 🐍 5. 串接 FastAPI (Astro-Service)

### 5.1 API 流程重構與 DTO 防護機制 (Security First)
嚴格遵守 `api_presenter.py` 規範：「**絕對不能把原始命理數據 (紫微星曜、八字十神) 吐給前端**」，否則用戶能透過 F12 逆向推導。
- **Onboarding (建立靈魂卡):** 發送單人資料至 `astro-service`，將算出來的西占、八字與紫微 JSON 存入 Supabase `soul_cards.natal_cache`。
- **安全轉譯 (DTO):** 所有的 `POST /api/matches/compute` 或 `/api/users/onboard` 回傳值，必須強制通過 `format_safe_match_response` 或 `format_safe_onboard_response` 進行清洗。前端只接 UI 標籤、分數 (Integers) 與 LLM 產出的字串。

### 5.2 串流渲染 (Streaming UI) 與 Token 鎖定防呆
由於牽涉到 LLM 生成（約需 10-20 秒，尤其是靈魂報告），這在 Vercel Hobby 方案會觸發 10 秒 Timeout 限制。解決方案：
1. **Frontend 制流:** 點擊「解析」按鈕後立即鎖住，並顯示神秘的「星盤轉動」或「塔羅洗牌」動畫。
2. **Streaming JSON (免 Redis MVP 解法):** 在 Next.js App Router 中，將處理 LLM 請求的 API Route 設定為 Streaming 模式 (e.g. Vercel AI SDK 的 `streamObject`)。讓前端「邊接收 JSON 的屬性，邊逐步渲染卡片文字」，出現打字機特效，大幅提升互動感並完美繞過 Vercel 10s Timeout。
3. **Token Pending Lock:** 在發起請求時，先將 1 枚代幣標記為「鎖定 (Pending)」。等 FastAPI 與 LLM 確定 `200 OK` 且存入 DB 後才正式扣除 (Commit)。若中途 Timeout 斷線，則解開鎖定還給用戶，避免客訴。

### 5.3 單人報告模式選擇 (Onboarding Selection)
參考 `prompt_manager.py`，單人建卡成功時，前端應提供**兩種解析方向**讓用戶二選一（增加互動樂趣）：
- 👉 **「我的隱藏靈魂面具」** (呼叫 `get_profile_prompt` / Tab C 模式，主攻挖掘陰暗面與自我蛻變)
- 👉 **「我的命中注定輪廓」** (呼叫 `get_ideal_match_prompt` / 提取婚神星與下降宮位，告訴用戶該找什麼人)

### 5.4 防禦巴納姆效應 (Anti-Barnum Effect) 的 Prompt 規範
為了保證解析結果精準、犀利且「不會套用在每個人身上都準」，必須在 LLM 端與 Prompt 嚴格把關：
1. **隱藏所有命理術語:** LLM 絕對不准輸出「紫微化忌、八字空亡、金火四分相」。所有底層運算必須被翻譯成純粹的「行為、恐懼、匱乏、需求」。
2. **強制使用因果公式 (Toxic Trap):** 開發者必須在 Prompt 中強制 LLM 套用公式：`因為此人極度需要 [X]，所以絕對無法忍受對方 [Y行為]，這會觸發其 [Z恐懼]`。例如：「因為你需要掌控感，無法忍受伴侶說走就走不計畫，這會觸發你的失控焦慮。」
3. **拒絕通用廢話:** 嚴格禁止 LLM 生成「自私、花心、愛說謊、脾氣差」等任何人都討厭的詞。必須描寫出「對別人是優點，對你卻是毒藥」的特質 (例如：「過度追求自由與距離感」對風象星座是優點，但對巨蟹屬性的人就是致命毒藥)。

### 5.5 Matchmaking (雙人合盤)
1. Next.js 直接從 Supabase 打撈 A 和 B 的 `natal_cache` JSON。
2. 發送包含這兩包 JSON 的 Request 給 `astro-service` 的 `/compute-match`。
3. `astro-service` 不重算星曆，直接跑配對邏輯並產出 Prompt，同時呼叫 LLM 取得報告。
4. Next.js 將結果存入 `matches` 資料表，並扣除代幣。
5. 重新導向至解析結果頁面 (`/report/[match_id]`)。

---

## 🔗 6. 社交裂變與問卷 (Viral Loop & Growth Hacking)

### 6.1 動態分享圖 (OG Image) 與一鍵發限動 (IG Story)
- **Open Graph (OG) 動態生成:** 利用 Next.js `@vercel/og`，根據配對的 `harmony_score` 自動產生帶有「A 與 B 的靈魂共振高達 99%！」大字的預覽縮圖，提升 Line / WhatsApp 點擊率。
- **IG 限動截圖匯出:** 在解析結果頁加入「下載解析卡片 (Save for Instagram)」按鈕，利用 `html2canvas` 讓前端把 3D 卡牌截圖成直式手機桌布比例，供用戶發限動炫耀。

### 6.2 唯讀分享頁面 (`src/app/share/[shareToken]/page.tsx`)
- 非登入用戶也能瀏覽的唯讀塔羅牌報告。
- 底部顯示 CTA：「想要算你跟別人的命運嗎？註冊立領 1 枚免費代幣 (Continue with Google)」。

### 6.3 邀請碼與補充代幣機制
- **雙贏推薦碼 (Referral):** 增加「邀請朋友註冊，雙方各得 3 枚代幣」機制，加速用戶自然增長。
- 當用戶代幣用盡時，彈出問卷 Modal (整合 Dialog 元件)。
- 用戶回答這份合盤的準確度。
- 送出表單後，呼叫 API 增加 1 枚 Token，回饋數據存入 `survey_feedbacks`。

---

## � 7. 資料庫安全性與部署策略

### 7.1 Supabase RLS 防禦 (Row Level Security)
- 必須在 Supabase 設定 RLS Policies 嚴格防範資料越權讀取：
  - `soul_cards` 與 `user_psychology_profiles`: 必須強制設定 `auth.uid() = owner_id`，確保用戶只能讀取自己的私密命盤與弱點標籤。
  - `matches`: 只能透過帶有專屬 `share_token` 的條件，或雙方當事人的 ID 來讀取。

### 7.2 部署平台配置
- **平台:** Vercel (Hobby Free Tier)
- **環境變數設定:**
  - `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - `ASTRO_SERVICE_URL` (指向 Railway)
  - `GOOGLE_AI_API_KEY` 或 `ANTHROPIC_API_KEY` (給 Streaming Route 用)

### 7.2 後端 (Python FastAPI) -> Railway
- **平台:** Railway ($5/月 Hobby Plan)
- **優勢:** Python FastAPI 與 `pyswisseph` (C-extension) 在 Railway 上能無縫編譯執行 (Vercel 不支援 C-extension)。
- **設定:** 根目錄需具備 `Procfile` (`web: uvicorn main:app --host 0.0.0.0 --port $PORT`) 與正確的 `requirements.txt`。

### 7.3 資料庫與 Auth -> Supabase
- **平台:** Supabase (Free Tier)
- **連線:** 前後端皆透過 Supabase Client 讀寫 PostgreSQL。

---

## 🎯 建議開發順序 (Phase Milestones)

- [ ] **Phase 1:** Supabase schema 建置與 Google OAuth 登入整合。
- [ ] **Phase 2:** 實作 `<TarotCard />` 3D 翻轉組件與命理視覺化 UI。
- [ ] **Phase 3:** 串接 `astro-service`，完成「單人建卡」與「雙人合盤」的 Server Actions 流程。
- [ ] **Phase 4:** 實作代幣扣除機制、問卷解鎖代幣與對外分享連結頁面。
